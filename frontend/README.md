快速启动前端（需要 Node.js >=16）

1. 进入前端目录并安装依赖：

```bash
cd frontend
npm install
```

2. 启动开发服务器：

```bash
npm run dev
```

页面地址通常为 `http://localhost:5173`。

说明：这是一个最小 DCIM 演示视图；后续我可以将其对接后端 API 动态渲染机房/机柜/服务器状态。
